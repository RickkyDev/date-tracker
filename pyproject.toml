[project]
name = "date-tracker"
version = "1.2.0"
description = "A customizable countdown widget for Windows."
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.8",
]

[project.scripts]
date-tracker = "main:main"